name: CI/CD Example

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Rodar verificação (CI)
        run: echo "CI executado com sucesso!"

      - name: Simular deploy (CD)
        run: echo "Deploy automatizado simulado com sucesso!"
